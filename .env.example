# CommCare Forge Environment Variables (for development only)
# The desktop app stores settings in encrypted local storage — you don't need this file to run the app.

# Your Anthropic API key (only needed if running from source in dev mode)
ANTHROPIC_API_KEY=sk-ant-...
